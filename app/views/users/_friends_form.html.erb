<% if current_user.is_friend? user %>
	<%= link_to 'Unfriend', user_friend_path(user), method: :delete %>
<% else %>
	<%= link_to 'Add friend', user_friends_path(user_id: current_user, friend_id: user.id), method: :create %>
<% end %>